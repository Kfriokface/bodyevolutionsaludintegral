tue<?php


// ——— Ajustes generales que sí pueden ir aquí ———
define('DB_CHARSET', 'utf8');
define('DB_COLLATE', '');
$table_prefix = 'wp_'; // AJUSTA si tu prefijo no es wp_

// —— CARGA EL ENV MUY PRONTO ——
$env_path = __DIR__ . '/wp-config.env.php';
if (is_readable($env_path)) {
  require_once $env_path;   // Aquí dentro defines DB_NAME, DB_USER, DB_PASSWORD, DB_HOST, WP_HOME, etc.
} else {
  die('No se pudo cargar wp-config.env.php');
}

// ——— Debug/log opcional ———
define('WP_DEBUG', false);
define('WP_DEBUG_LOG', false);
define('WP_DEBUG_DISPLAY', false);
@ini_set('display_errors', 0);

// ——— Bootstrap estándar de WP ———
if (!defined('ABSPATH')) {
  define('ABSPATH', __DIR__ . '/');
}
require_once ABSPATH . 'wp-settings.php';

